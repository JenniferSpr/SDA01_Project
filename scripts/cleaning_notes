---- To be added on the main notebook ----

# Missing values : None. The only NAs are in wc2026_exit_round (expected);
which marks countries that didn't take part in the WC

# Duplicate snapshots : 2026 rows contained two snapshots for 48 countries
(2026-07-07 & 2026-12-31). July is the pre/mid-WC* so it was extracted into a
separate file "elo_ratings_wc.csv" to keep it from mixing with the year-end data.
The main dataset "elo_ratings" holds a single snapshot per country per year

# Consistency check : 
1. Verified that match counts add up (matches total = matches home +
matches away + matches neutral) and also equals wins + losses + draws
2. No negative values
3. Range dates as expected (1901 - 2026)

# New datasets 
elo_ratings.csv : 4635 observations, 24 variables
wc_elo_ratings.csv : 48 observations, 24 variables


* Maybe we'll have to retrieve data for the whole WC ?








